@model ASP.NET_MVC_MajsterStrelby.Models.PlayersViewModel
@{
    ViewBag.Title = "Rebríček";
}
<div class="jumbotron">
    <h2>Leaderboard</h2>
    <ul>
        @foreach (var player in Model.GetPlayers())
        {
            <li>
                @player._name , @player._points , @player._resolved
            </li>
        }
    </ul>
</div>
